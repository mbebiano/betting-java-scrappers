package com.superodds.domain.model;

/**
 * Periods within a match that a market can refer to.
 */
public enum PeriodType {
    REGULAR_TIME,
    FIRST_HALF,
    SECOND_HALF
    // Future: EXTRA_TIME, PENALTIES, etc.
}
