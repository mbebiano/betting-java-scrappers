package com.superodds;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main Spring Boot application for the betting scrapers microservice.
 */
@SpringBootApplication
public class BettingScrapersApplication {

    public static void main(String[] args) {
        SpringApplication.run(BettingScrapersApplication.class, args);
    }
}
